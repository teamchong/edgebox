# Zig build artifacts
zig-out/
# Exception: commit SHARED frozen_functions files (skip 18min compile time)
!zig-out/cache/
!zig-out/cache/frozen_functions.c
!zig-out/cache/frozen_functions.o
!zig-out/cache/frozen_functions.sha256
zig-cache/
.zig-cache/

# Node modules
node_modules/
**/node_modules/

# Package files generated by build.sh
examples/*/package.json
examples/*/bun.lock

# JavaScript build artifacts
dist/
build/
*.js.map
*.d.ts

# npm/pnpm/yarn lock files
package-lock.json
pnpm-lock.yaml
yarn.lock

# Build artifacts (generated by build.sh)
*.aot
*.wasm
*_porffor
bundle.js
bundle_compiled.c
.last-build
.edgebox.json
!vendor/**/*.wasm

# Large generated bundles (Claude CLI builds)
claude_bundle.js
claude_full.js
sdk_with_helpers.js

# Test artifacts
coverage/
.nyc_output/
test262/
vendor/quickjs-ng/test262/

# IDE
.vscode/
.idea/

# OS
.DS_Store
Thumbs.db

# Temp files
*.tmp
*.temp
*.log
*.dylib
*.so
*.a
*.o
*.cache

# Vendor submodule build artifacts (never commit upstream changes)
vendor/*
# Exception: commit prebuilt libraries for faster CI builds
!vendor/prebuilt/
!vendor/prebuilt/**

# Test files should be in test/ directory
/test_*.js.env
.env
*.o
frozen_manifest.json
bundle_original.c
.repl_history

# Generated build files (now in zig-out/, but kept here for backwards compatibility)
bundle_compiled.c
frozen_functions.c
claude_bundle_compiled.c
*.rej
node-core/
